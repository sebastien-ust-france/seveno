import { CandidateFeatureComingSoon } from '@/components/candidate/CandidateFeatureComingSoon';

export default function CandidateOffersPage() {
  return (
    <CandidateFeatureComingSoon
      title="Offres pour mes métiers"
      description="La consultation des offres correspondantes sera ouverte lors du lancement complet de Seven’O."
      backLabel="Retour au tableau de bord candidat"
    />
  );
}
